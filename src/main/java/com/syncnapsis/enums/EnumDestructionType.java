package com.syncnapsis.enums;

public enum EnumDestructionType
{
	destroyed, // totally destroyed by opponent
	givenUp, // given up by player itself
	starved, // population decreased to 0 due to negative growth rate
}
